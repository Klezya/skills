# Session 2026-03-02

## Objetivo
Evaluar si la skill `fastapi-best-practices` estaba bien enfocada, decidir si dividirla en sub-skills, crear las nuevas skills y mejorar `install.sh`.

## Contexto activo
- Rama: `main`
- Stack: Bash, Markdown, Python (ejemplos)
- Proyecto: skills (Copilot Skills Registry)

## Cambios

### `.agents/skills/web-security/SKILL.md` — NUEVO
- Skill de seguridad web framework-agnostic (519 líneas)
- OWASP Top 10 con mitigaciones y código, security headers, rate limiting, password hashing, JWT policy, secrets management, SSRF prevention

### `.agents/skills/web-security/assets/security_headers_middleware.py` — NUEVO
- Middleware Starlette/FastAPI con todos los security headers OWASP (X-Content-Type-Options, X-Frame-Options, CSP, HSTS opcional)

### `.agents/skills/web-security/assets/rate_limiting.py` — NUEVO
- Setup completo de slowapi: key functions (IP, user), limiter, exception handler, ejemplos por endpoint

### `.agents/skills/web-security/assets/password_hashing.py` — NUEVO
- argon2id (preferido) + bcrypt (alternativa) + PasswordService unificado

### `.agents/skills/production-deployment/SKILL.md` — NUEVO
- Skill de deployment framework-agnostic (532 líneas)
- Nginx reverse proxy + TLS, Docker multi-stage, docker-compose, health checks, environment strategy, logging JSON, Prometheus monitoring

### `.agents/skills/production-deployment/assets/nginx.conf` — NUEVO
- Config nginx producción: TLS, security headers, rate limiting por zona, proxy buffering

### `.agents/skills/production-deployment/assets/Dockerfile` — NUEVO
- Multi-stage build Python + uv, non-root user, HEALTHCHECK

### `.agents/skills/production-deployment/assets/docker-compose.yml` — NUEVO
- Stack completo: app + PostgreSQL 16 + nginx con healthchecks y redes separadas

### `.agents/skills/production-deployment/assets/health_check.py` — NUEVO
- Endpoints /health (liveness) y /ready (readiness) con verificación de DB

### `.agents/skills/fastapi-best-practices/SKILL.md` — MODIFICADO
- Agregadas cross-references a `web-security` y `production-deployment`
- Corregida referencia OWASP A02 → A05

### `AGENTS.md` — MODIFICADO
- Agregadas secciones 🔒 Security y 🚀 Deployment con las nuevas skills

### `install.sh` — MODIFICADO
- Cambiado de symlink a generación de AGENTS.md desde template
- `generate_agents_md()`: copia AGENTS.template.md al **root del proyecto** con tabla de skills dinámica
- `remove_agents_md()`: elimina el AGENTS.md generado en uninstall
- Se genera en raíz del proyecto, no en `.agents/`

## Resumen
Se decidió no dividir `fastapi-best-practices` sino crear skills complementarias por capa: `web-security` (OWASP, headers, rate limiting, passwords, JWT policy) y `production-deployment` (nginx, Docker, TLS, health checks, monitoring). Las 3 skills se cross-referencian. `install.sh` ahora genera `AGENTS.md` en la raíz del proyecto desde el template, con la lista de skills instaladas auto-poblada.

## Pendiente para mañana
- [ ] Commit de todos los cambios nuevos
- [ ] `./install.sh --global` para re-symlink con las nuevas skills
- [ ] Revisar si `redis-cache` skill vale la pena crearla ahora
- [ ] Considerar agregar más assets a `production-deployment` (ej: GitHub Actions CI/CD workflow)
